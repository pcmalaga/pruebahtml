name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Scripts
        run: |
          docker build -t pruebahtml .
          docker login -u pcmalaga -p dckr_pat_f-BJfR1XhIEwIr60crnATYzlX2I
          docker tag pruebahtml pcmalaga/pruebahtml
